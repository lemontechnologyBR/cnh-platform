# API
JWT_SECRET=troque_por_um_segredo_forte
MP_ACCESS_TOKEN=
MP_WEBHOOK_URL=https://seudominio.com/api/recharges/webhook
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Porta do nginx (app + admin + proxy /api)
WEB_PORT=8080
